fn fib(n) {
    if (n < 2)  {        
        return n
    }
    nMinOne: int
    nMinOne = n - 1
    nMinTwo: int
    nMinTwo = n - 2
    return fib(nMinOne) + fib(nMinTwo)
}

fib(5)


print "something else" 