struct arrayHelper() {
    fn getArrLen(arr) {
        return len(arr)
    }
}

a: list -> [1, 2, 3, 4, 5]

b -> arrayHelper.getArrLen(a)

if (b == 5) {
    print "hehe xd"
} else {
    print "shouldnt happen"
}