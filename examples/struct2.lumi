struct arrayHelper() {
    b: int -> 2
    fn getArrLen(arr) {
        return arr.len() + b
    }
}

a: list -> [1, 2, 3, 4, 5]

b: int -> arrayHelper.getArrLen(a)

print b

if (b == 5) {
    print "hehe xd"
} else {
    print "shouldnt happen"
}