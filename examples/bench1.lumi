fn weirdPara(n) {    
    i: 1 to n step 1 {
        // print i
        doRandomStuff(i, i - 1)
    }
    print "done :) "
}

fn doRandomStuff(a, b) {
    c: int
    c = a * b + 2
    // print c
}

n: int -> 1000000
weirdPara(n - 1)